build:
  maxIssues: 0

config:
  validation: true

processors:
  active: true

comments:
  CommentOverPrivateFunction:
    active: false

naming:
  FunctionNaming:
    active: true
    functionPattern: '^([a-z][a-zA-Z0-9]*|[A-Z][a-zA-Z0-9]*)$'
