<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme. -->
    <style name="Theme.AuraTrackr" parent="android:Theme.Material.Light.NoActionBar" />

    <!--
    1. Manually define the transparent style, inheriting from the base Theme.AppCompat.
    This is the robust solution you provided.
    -->
    <style name="Theme.AppCompat.Transparent.NoActionBar" parent="Theme.AppCompat">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:backgroundDimEnabled">false</item>
    </style>

    <!--
    2. Then, have our app's specific transparent theme inherit from the style we just created.
    -->
    <style name="Theme.AuraTrackr.Transparent" parent="Theme.AppCompat.Transparent.NoActionBar" />

</resources>
